---
# ============================================================
# Play 1: Dynamiczne dodanie hosta
# ============================================================
- name: Add target host dynamically
  hosts: localhost
  connection: local
  gather_facts: false
  tasks:
    - name: Add pihole server to inventory
      add_host:
        name: "{{ lookup('env', 'INSTANCE_IP') }}"
        groups: pihole_servers
        ansible_user: "{{ lookup('env', 'SSH_USER') | default('ansible', true) }}"
        ansible_ssh_private_key_file: "{{ lookup('env', 'SSH_KEY_PATH') | default('/mnt/workspace/id_ed25519', true) }}"
        ansible_ssh_common_args: "-o StrictHostKeyChecking=no"
      vars:
        instance_ip: "{{ lookup('env', 'INSTANCE_IP') }}"
      failed_when: not instance_ip

# ============================================================
# Play 2: Instalacja i konfiguracja Pi-hole v6 + WireGuard + Let's Encrypt
# ============================================================
- name: Setup Pi-hole with WireGuard and Let's Encrypt HTTPS
  hosts: pihole_servers
  become: true
  gather_facts: true
  vars:
    pihole_password: "{{ lookup('env', 'PIHOLE_PASSWORD') | default('ChangeMe123!', true) }}"
    domain_name: "{{ lookup('env', 'DOMAIN_NAME') | default('', true) }}"
    email: "{{ lookup('env', 'USER_EMAIL') | default('admin@example.com', true) }}"
    pihole_interface: "{{ lookup('env', 'PIHOLE_INTERFACE') | default('ens4', true) }}"
    wireguard_port: "{{ lookup('env', 'WIREGUARD_PORT') | default('51820', true) }}"
    wireguard_clients: "{{ lookup('env', 'WIREGUARD_CLIENTS') | default('2', true) | int }}"
    pihole_tls_pem: "/etc/pihole/tls.pem"
    pihole_webroot: "/etc/pihole/www"

  tasks:
    - name: Wait for connection (OS Login propagation)
      wait_for_connection:
        delay: 10
        timeout: 120

    # --- JEDEN TASK: update + upgrade + install ---
    - name: Install all required packages and upgrade system
      apt:
        name:
          - curl
          - wget
          - git
          - ufw
          - certbot
          - wireguard-tools
          - qrencode
          - iptables-persistent
          - wireguard
          - mc
        state: present
        update_cache: yes
        cache_valid_time: 3600
      register: apt_result
      retries: 3
      delay: 10
      until: apt_result is succeeded

    # --- Pi-hole ---
    - name: Check if Pi-hole is already installed
      stat:
        path: /usr/local/bin/pihole
      register: pihole_installed

    - name: Download Pi-hole installer
      get_url:
        url: "https://install.pi-hole.net"
        dest: /tmp/pihole-install.sh
        mode: '0755'
      when: not pihole_installed.stat.exists

    - name: Create Pi-hole setup vars file
      copy:
        content: |
          PIHOLE_INTERFACE={{ pihole_interface }}
          IPV4_ADDRESS=$(ip -4 addr show {{ pihole_interface }} | grep -oP '(?<=inet\s)\d+(\.\d+){3}')
          IPV6_ADDRESS=
          PIHOLE_DNS_1=1.1.1.1
          PIHOLE_DNS_2=1.0.0.1
          QUERY_LOGGING=true
          WEBPASSWORD={{ pihole_password }}
          BLOCKING_ENABLED=true
        dest: /tmp/setupVars.conf
      when: not pihole_installed.stat.exists

    - name: Install Pi-hole unattended
      shell: |
        mkdir -p /etc/pihole
        cp /tmp/setupVars.conf /etc/pihole/setupVars.conf
        bash /tmp/pihole-install.sh --unattended
      args:
        creates: /usr/local/bin/pihole
      when: not pihole_installed.stat.exists
      register: pihole_install
      failed_when: pihole_install.rc not in [0, 1]

    # --- Let's Encrypt + TLS ---
    - name: Ensure Pi-hole webroot for certbot
      file:
        path: "{{ pihole_webroot }}/.well-known/acme-challenge"
        state: directory
        owner: pihole
        group: pihole
        mode: '0755'
      when: domain_name | length > 0

    - name: Obtain Let's Encrypt certificate
      command: >
        certbot certonly --webroot -w {{ pihole_webroot }}
        --non-interactive --agree-tos --email {{ email }} -d {{ domain_name }}
      args:
        creates: /etc/letsencrypt/live/{{ domain_name }}/fullchain.pem
      register: certbot_result
      failed_when: certbot_result.rc not in [0, 1]
      when: domain_name | length > 0

    - name: Stat LE certificate and TLS PEM
      stat:
        path: "{{ item }}"
      loop:
        - "/etc/letsencrypt/live/{{ domain_name }}/fullchain.pem"
        - "{{ pihole_tls_pem }}"
      register: tls_files
      when: domain_name | length > 0

    - name: Combine private key + fullchain into PEM for Pi-hole v6
      shell: |
        set -e
        cat /etc/letsencrypt/live/{{ domain_name }}/privkey.pem \
            /etc/letsencrypt/live/{{ domain_name }}/fullchain.pem > {{ pihole_tls_pem }}.tmp
        mv {{ pihole_tls_pem }}.tmp {{ pihole_tls_pem }}
      when:
        - domain_name | length > 0
        - tls_files.results[0].stat.exists | default(false)
        - not tls_files.results[1].stat.exists | default(false)

    - name: Set ownership on TLS PEM
      file:
        path: "{{ pihole_tls_pem }}"
        owner: pihole
        group: pihole
        mode: '0600'
      when:
        - domain_name | length > 0
        - tls_files.results[1].stat.exists | default(false)

    - name: Configure Pi-hole v6 domain and TLS
      command: "pihole-FTL --config {{ item.key }} {{ item.value }}"
      loop:
        - { key: "webserver.domain", value: "{{ domain_name }}" }
        - { key: "webserver.tls.cert", value: "{{ pihole_tls_pem }}" }
      register: ftl_config
      changed_when: "'set' in ftl_config.stdout"
      when:
        - domain_name | length > 0
        - tls_files.results[1].stat.exists | default(false)

    - name: Create certbot deploy hook for auto-renewal
      copy:
        dest: "/etc/letsencrypt/renewal-hooks/deploy/99-pihole.sh"
        content: |
          #!/bin/bash
          cat /etc/letsencrypt/live/{{ domain_name }}/privkey.pem \
              /etc/letsencrypt/live/{{ domain_name }}/fullchain.pem > {{ pihole_tls_pem }}
          chown pihole:pihole {{ pihole_tls_pem }}
          chmod 600 {{ pihole_tls_pem }}
          systemctl restart pihole-FTL
        mode: '0755'
      when:
        - domain_name | length > 0
        - tls_files.results[0].stat.exists | default(false)

    # --- JEDEN RESTART FTL ---
    - name: Restart pihole-FTL to apply all changes
      systemd:
        name: pihole-FTL
        state: restarted
      when: not ansible_check_mode

    # --- WireGuard ---
    - name: Enable IP forwarding
      sysctl:
        name: net.ipv4.ip_forward
        value: '1'
        state: present
        reload: yes

    - name: Check if WireGuard config exists
      stat:
        path: /etc/wireguard/wg0.conf
      register: wg0_conf

    - name: Generate WireGuard server keys
      shell: |
        PRIVATE=$(wg genkey)
        PUBLIC=$(echo "$PRIVATE" | wg pubkey)
        echo "PRIVATE=$PRIVATE"
        echo "PUBLIC=$PUBLIC"
      register: wg_keys
      changed_when: false
      no_log: true
      when: not wg0_conf.stat.exists

    - name: Create WireGuard server config
      copy:
        content: |
          [Interface]
          Address = 10.8.0.1/24
          ListenPort = {{ wireguard_port }}
          PrivateKey = {{ wg_keys.stdout_lines[0] | regex_replace('PRIVATE=', '') }}
          PostUp = iptables -A FORWARD -i wg0 -j ACCEPT; iptables -t nat -A POSTROUTING -o {{ pihole_interface }} -j MASQUERADE
          PostDown = iptables -D FORWARD -i wg0 -j ACCEPT; iptables -t nat -D POSTROUTING -o {{ pihole_interface }} -j MASQUERADE
        dest: /etc/wireguard/wg0.conf
        mode: '0600'
      when: not wg0_conf.stat.exists
      no_log: true

    - name: Ensure WireGuard clients directory
      file:
        path: /etc/wireguard/clients
        state: directory
        mode: '0700'

    - name: Generate WireGuard client configurations
      include_tasks: wireguard_client.yml
      loop: "{{ range(1, wireguard_clients + 1) | list }}"
      loop_control:
        loop_var: client_number
      when: not wg0_conf.stat.exists

    - name: Start and enable WireGuard
      systemd:
        name: wg-quick@wg0
        daemon_reload: yes
        state: started
        enabled: yes
      when: not ansible_check_mode

    # --- UFW ---
    - name: Configure UFW
      ufw:
        rule: "{{ item.rule | default('allow') }}"
        direction: "{{ item.direction | default('in') }}"
        proto: "{{ item.proto | default('tcp') }}"
        port: "{{ item.port }}"
      loop:
        - { port: '22' }
        - { port: '80' }
        - { port: '443' }
        - { port: '53', proto: 'udp' }
        - { port: '{{ wireguard_port }}', proto: 'udp' }
      notify: reload ufw

    - name: Set UFW default policies and enable
      ufw:
        state: enabled
        policy: deny
        direction: incoming

    # --- Podsumowanie ---
    - name: Collect service statuses
      shell: |
        echo "=== Pi-hole ==="
        pihole -v 2>/dev/null || true
        echo "=== WireGuard ==="
        systemctl is-active wg-quick@wg0 2>/dev/null || echo "inactive"
      register: summary
      changed_when: false

    - name: Display summary
      debug:
        msg:
          - "Pi-hole: https://{{ domain_name }}/admin (HTTP: http://{{ ansible_default_ipv4.address }}/admin)"
          - "WireGuard configs: /etc/wireguard/clients/"
          - "{{ summary.stdout_lines }}"

  handlers:
    - name: reload ufw
      ufw:
        state: reloaded

# ============================================================
# Play 3: Szybka weryfikacja
# ============================================================
- name: Quick verification
  hosts: pihole_servers
  become: true
  gather_facts: false
  vars:
    pihole_ip: "{{ ansible_host | default(lookup('env','INSTANCE_IP') | default('127.0.0.1')) }}"
    domain_name: "{{ lookup('env', 'DOMAIN_NAME') | default('', true) }}"

  tasks:
    - name: Verify DNS and services (combined)
      shell: |
        echo "--- DNS LOCAL ---"
        dig @127.0.0.1 +short google.com || true
        echo "--- PORTS ---"
        ss -tulpn | grep -E '(:53|:80|:443)' || true
        echo "--- FTL STATUS ---"
        systemctl is-active pihole-FTL || true
        echo "--- VERSION ---"
        pihole -v 2>/dev/null || true
      register: verify
      changed_when: false
      failed_when: false

    - name: Check HTTPS admin panel
      uri:
        url: "https://{{ domain_name }}/admin"
        status_code: 200
        validate_certs: yes
      register: web_admin
      ignore_errors: true
      when: domain_name | length > 0

    - name: Show verification results
      debug:
        var: verify.stdout_lines