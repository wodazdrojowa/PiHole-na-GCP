#stack:
#  ansible:
#    playbook: pihole-setup.yml  # nazwa Twojego głównego playbooka
#  steps:
#    before_plan:
#      - ansible-galaxy install -r requirements.yml -vvvv
#    before_apply:
#      - ansible-galaxy install -r requirements.yml -vvvv